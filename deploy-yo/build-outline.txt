
# process outline:
# - pull the samples repo
# - compile the yo cordapp with gradle (not using sample nodes, so no need for deployNodes)
# - pull andrea, barbara and notary node.conf
# - pull the bootstrapper
# - bootstrap the network
# - create directories for: notary, andrea, barbara
#   - add bootstrapped artifacts
#   - add dockerfiles
#   - add jolokia
#   - add cordapp
# - build andrea as andrea-yo, tag as irvnet/andrea-yo, push as irvnet/andrea-yo
# - build barabara as barbara-yo, tag as irvnet/barbara-yo, push as irvnet/barbara-yo
# - build notary as notary-yo, tag as irvnet/notary-yo, push as irvnet/notary-yo

